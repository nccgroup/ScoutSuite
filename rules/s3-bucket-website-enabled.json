{
    "dashboard_name": "Bucket",
    "description": "Bucket with static website enabled",
    "entities": "s3.buckets.id",
    "conditions": [ "and",
        [ "s3.buckets.id.web_hosting", "equal", "Enabled" ]
    ],
    "id_suffix": "web_hosting",
    "level": "warning"
}
